package com.patri.java.ocp._2_design_patterns_and_principles._2_functional_programming;

public class Intro {
    // ■■ Functional interface = interface that contains a single abstract method
    // functional interfaces are used for lambda expressions
    // functional interfaces is a new feature that has been added to Java 8

    // ■■ Lambda expression = block of code that is used as a anonymous method
    // another way to see a lambda expression = method wrapped within an object - it's used instead of an anonymous inner class

    // lambda expression -> uses deferred execution = code is specified now but runs later
    // ( later = when the method that uses the lambda expression is used )

    // ■■ Defining a Functional Interface
    // example: Sprint.java & Tiger.java

    // ■■ Implementing functional interfaces with lambdas
    // example: Animal.java + CheckTrait.java + FindMatchingAnimals.java

    // ■ Understand lambda syntax
    // example: LambdaSyntax.java

    // ■ Spotting invalid Lambdas
    // example: InvalidLambdas.java

    // ■ Applying the Predicate Interface
    // example: UsingPredicateInterface.java
}
